# تقييم مشروع CarRental وبناء لوحة تحكم إدارة احترافية (MVC)

**المشروع:** [AnasAlShmiri/CarRental](https://github.com/AnasAlShmiri/CarRental) على فرع `mvc-dashboard`
**الإعداد:** Manus AI — 13 أغسطس 2026

## أولاً: تقييم المشروع الأصلي

بعد فحص شامل لبنية المشروع الأصلي، فإن **جودته عالية جدًا** ويستحق أن يكون نموذجًا احتيائيًا لمشروع .NET. يعتمد المشروع على **البنية النظيفة (Clean Architecture)** بأربع طبقات منفصلة بحدود واضحة: طبقة `Domain` التي تحمل كيانات الأعمال (السيارة `Car`، العميل `Customer`، الإيجار `Rental`) وحالاتها (`CarStatus` و`RentalStatus`) كسلاسل ثابتة النوع، وطبقة `Application` التي تعرّف الواجهات المجردة (مستودعات البيانات، خدمة التوكنات، تخزين الصور، واجهات الإحصائيات)، وطبقة `Infrastructure` التي تقدم تنفيذات فعلية (Entity Framework Core مع مستودعات حقيقية، `LocalCarImageStorage` الذي يتحقق من البايّتات السحرية للملفات قبل قبولها لمنع رفع سكربتات خبيثة باسم صور)، وأخيرًا `CarRental.API` التي تقدم نقاط النهاية.

| المعيار | التقييم | الملاحظات |
|---|---|---|
| البنية المعمارية | ممتاز | Clean Architecture مع Repository Pattern وتبعية نظيفة (Domain لا تعتمد على شيء) |
| نماذج النطاق | قوية | قيود `DataAnnotation`، قيم افتراضية للحالات، مؤشر مركب على (CarId, Status) لفحص التداخل |
| قواعد الأعمال | سليمة | منع تداخل الإيجارات بفترة نصف مفتوحة، حساب السعر الإجمالي في الخادم، رفض الأرقام العشرية بالفواصل لمنع فساد البيانات |
| الأمان | جيد جدًا | تحقق من نوع الملف الفعلي (magic bytes)، فحص JWT، توقيعات مصادقة على API |
| الاختبارات | ممتاز | 101 اختبار وحدات يمر بنجاح + مجموعة Postman في `docs/` |
| التوثيق | ممتاز | README عربي مفصل يشرح البنية وسير العمل |

خلاصة التقييم: المشروع جاهز للإنتاج من ناحية النطاق والطبقات التحتية، وكانت المهمة هي إضافة واجهة إدارة بشرية فوقه دون تعديل في تلك الطبقات — وهو ما تحقق بالكامل.

## ثانياً: لوحة التحكم الجديدة `CarRental.Web`

أُضيف مشروع MVC سابع ضمن الحل نفسه يعيد استخدام طبقات `Domain` و`Application` و`Infrastructure` كما هي، فلا يوجد أي تكرار للمنطق ولا أي تعديل في قواعد الأعمال الأصلية. الدخول يتم ببيانات الأدمن نفسها (`admin` / `Admin@12345`) لكن عبر **مصادقة كوكيز** مع حماية CSRF (Antiforgery) على كل النماذج، في تجربة خادمية كاملة بدلًا من JWT.

### مكونات اللوحة

| الوحدة | المزايا |
|---|---|
| لوحة المعلومات (Dashboard) | أربع بطاقات إحصائية (إجمالي السيارات، السيارات المؤجرة، العملاء، الإيجارات النشطة)، ملخص حالة الإيجارات (نشط/مكتمل/ملغى)، ورسم بياني تفاعلي للإيراد الشهري بـ Chart.js بأطر 3 و6 و12 شهرًا |
| إدارة السيارات | CRUD كامل مع **رفع صور آمن** (التحقق من البايّتات السحرية وحد الحجم، حفظ بأسماء UUID فريدة في `wwwroot/uploads`)، تحليل السعر يدويًا بصيغة `150.50` لتجنب مشاكل الثقافات، وإدارة حالة السيارة (متاحة/مؤجرة/صيانة) |
| إدارة العملاء | CRUD مع فحص تفرد البريد الإلكتروني وعدم الحذف عند وجود إيجارات |
| إدارة الإيجارات | إنشاء مع فرض عدم تداخل التواريخ، **إكمال الإيجار** (يُعيد السيارة للحالة المتاحة ويحسب الإجمالي)، إلغاء، وحذف مقيد |
| المصادقة | تسجيل دخول/خروج، صفحة وصول مرفوض، جلسة محفوظة 8 ساعات مع تمدد انزلاقي |

### التصميم

التصميم عربي بالكامل باتجاه RTL من صفحة `_Layout` (مستخدمًا `bootstrap-reboot.rtl.css` المخصص للمشروع)، بلوحة جانبية كحلية داكنة مع لمسات ذهبية وواجهة فاتحة للمحتوى، مع خطوط عربية مناسبة وأيقونات، وجميع النصوص والرسائل والتواريخ بالعربية.

## ثالثاً: مشكلة تقنية حُلّت أثناء البناء

واجهت مشكلة عميقة أثناء تطوير نماذج السيارات: كانت النماذج تعاد للصفحة فارغة تمامًا دون أي خطأ ظاهر، وسببها أن خاصية `Model` في `CarViewModel` تتعارض داخليًا مع خاصية `ControllerBase.Model` في ASP.NET Core أثناء ربط النماذج، فيتجاهل المربّط كل القيم بصمت. الحل كان إعادة تسمية الخاصية إلى `CarModel` وتحديث كل الواجهات، إضافة إلى تحليل السعر يدويًا عبر `decimal.TryParse` بثقافة Invariant لتخزين القيم بدقة دون فساد بفعل ثقافة الواجهة العربية.

## رابعاً: الاختبار النهائي

اُختبر سيناريو كامل تلقائيًا وبنجاح تام: تسجيل دخول الأدمن، إنشاء سيارة مع رفع صورة والتحقق من ظهورها في القائمة، إنشاء سيارة ثانية، إنشاء عميل، حجز إيجار (مع اختيار السيارة الجديدة لتفادي التداخل)، إكمال الإيجار وتحقق انتقال الحالة إلى "مؤج" مع الإحصائيات، وأخيرًا التحقق من لوحة المعلومات. البناء الكامل للحل يعمل بصفر أخطاء في وضع Release.

## خامساً: التشغيل محليًا

```bash
git clone https://github.com/AnasAlShmiri/CarRental.git
cd CarRental && git checkout mvc-dashboard
dotnet run --project CarRental.Web
```

المعاينة على `http://localhost:5110` بحساب `admin` / `Admin@12345`. المشروع يدعم المفاتيح التالية في `appsettings.json` أو المتغيرات البيئية: `DatabaseProvider` (`SqlServer` / `Sqlite` / `InMemory`) مع `ConnectionStrings__DefaultConnection`، و`AdminUser` لبيانات الأدمن. في بيئة التطوير على SQLite يكفي ضبط `ConnectionStrings__DefaultConnection` إلى ملف مثل `Data Source=carrental.db`.

## المراجع

1. [المستودع الأصلي على GitHub](https://github.com/AnasAlShmiri/CarRental) — فرع `mvc-dashboard`
2. [التقرير السابق لتقييم المشروع](https://github.com/AnasAlShmiri/CarRental) — ملاحظات الجلسة السابقة ضمن المستودع
