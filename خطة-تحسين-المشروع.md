# خطة تحسين مشروع تأجير السيارات CarRental

**مشروع التدريب الميداني — قسم تقنية المعلومات — جامعة الحكمة (2026–2027)**

> **إعداد: Manus AI**
> **المشروع: تطبيق ويب وMobile App لنظام تأجير السيارات**
> **المستودع: [AnasAlShmiri/CarRental](https://github.com/AnasAlShmiri/CarRental) — الفرع: `mvc-dashboard`**

---

## أولًا: تقييم المشروع الحالي مقابل متطلبات الدورة

قمت بمراجعة هيكل المشروع كاملًا ومقارنته بمتطلبات الدليل المرفق أسبوعًا بعد أسبوع. النتيجة الإجمالية ممتازة: المشروع يحقق معظم المتطلبات الإلزامية، ويوجد بعض الفجوات التي يمكن سدّها بترتيب أولويات محدد.

| المتطلب | الحالة الحالية | التقييم |
|---|---|---|
| ثلاث جداول مترابطة على الأقل | جداول Cars وCustomers وRentals مترابطة بمفاتيح خارجية (CarId, CustomerId) | ✅ مكتمل |
| جدول فيه صور | جدول Cars يحتوي ImageUrl مع رفع الصور من المتصفح ومعاينتها وحذفها | ✅ مكتمل |
| لوحة إحصائيات رئيسية | DashboardController + Chart.js بالإيراد الشهري وبطاقات الحالة | ✅ مكتمل |
| Views لكل كيان (Index/Details/Create/Edit/Delete) | موجود لكل الكيانات الثلاثة | ✅ مكتمل |
| التحقق من المدخلات (required, datatype, length, email) | DataAnnotations + Unobtrusive Validation + فلترة الفواصل العشرية | ✅ مكتمل |
| Bootstrap | Bootstrap 5 RTL كامل | ✅ مكتمل |
| Bootstrap Icons | **غير مستخدمة حاليًا** — الأيقونات حاليًا رموز نصية Unicode | ⚠️ مطلوب إضافتها |
| خطوط مخصصة | **غير مضافة** | ⚠️ مطلوب إضافة خط عربي (مثل Tajawal أو Cairo) |
| CSS Variables | موجودة في site.css | ✅ مكتمل |
| Bootstrap Modal | **غير موجودة** — الحذف والإجراءات حالياً صفحات منفصلة | ⚠️ مطلوب (بنداً إلزامياً) |
| DataTables.js | **غير موجودة** — الجداول حالياً HTML عادية | ⚠️ مطلوب (بنداً إلزامياً) |
| GitHub بفروع ودمج | master + develop + fix/* + mvc-dashboard + Merge PRs | ✅ مكتمل ومتفوق |
| Flutter App | **لم يبدأ بعد** (W8 — 15%) | ❌ غير موجود |
| ASP.NET Core 10 Web API | موجودة مع Swagger وJWT وقائمة Postman | ✅ مكتمل |

**الخلاصة:** نقاط القوة الحالية كبيرة (البنية النظيفة Clean Architecture، الفروع في GitHub، لوحة RTL احترافية)، والفجوات الموزعة على أربعة محاور: **Bootstrap Icons وخطوط مخصصة، Bootstrap Modal، DataTables.js، وتطبيق Flutter**.

---

## ثانيًا: خريطة التقييم (وزن كل مرحلة)

| الأسبوع | المكوّن | الوزن | المجهود المطلوب |
|---|---|---|---|
| W2 | مقترح المشروع | 5% | كتابة وثيقة — 90% جاهز |
| W3 | التصميم (Clean Architecture + مخططات) | 15% | رسم المخططات فقط — الكود جاهز |
| W4 | UI/UX (Figma/XD) | 5% | Wireframe تفاعلي فقط |
| W5 | Web API | 10% | ✅ موجود بالكامل |
| W6 | اختبار API (Swagger/Postman) | 5% | ⚠️ ينقص ملف حالات الاختبار المكتوبة |
| W7 | تطبيق MVC | **25%** | ✅ الواجهة موجودة — تحتاج Bootstrap Icons + Modal + DataTables + خط |
| W8 | تطبيق Flutter | **15%** | ❌ يحتاج بناء كامل |
| W9 | GitHub | 5% | ✅ ممتاز |
| W10 | التوثيق | 15% | ⚠️ يحتاج دليل تثبيت + دليل مستخدم + خلاصة |

**إجمالي ما تبقى فعليًا:** نحو 20% من درجة المشروع، ويمكن إنجازه في أسبوع واحد بترتيب الأولويات التالي.

---

## ثالثًا: خطة التنفيذ الموصى بها (مرتبة بالأولوية)

### المرحلة 1 — إغلاق فجوات MVC سريعًا (يوم واحد)

هذه هي أسرع فرصة للدرجة: إضافة العناصر الإلزامية الناقصة على الواجهة الموجودة دون تغيير المنطق.

1. **Bootstrap Icons:** إضافة رابط CDN في `_Layout.cshtml` واستبدال الرموز النصية (▣ ♙ ↔) بأيقونات رسمية (`bi-car-front-fill`, `bi-people-fill`, `bi-arrow-repeat`, `bi-speedometer2`).
2. **خط عربي مخصص:** إضافة خط **Tajawal** أو **Cairo** من Google Fonts في `<head>` وتطبيقه عبر CSS variable:

   ```css
   :root { --font-arabic: 'Tajawal', sans-serif; }
   body { font-family: var(--font-arabic); }
   ```

3. **Bootstrap Modal:** تحويل عمليات **الحذف** و**إكمال/إلغاء الإيجار** إلى نافذة Modal بدلاً من صفحات منفصلة، مع إبقاء صفحات Details الحالية.
4. **DataTables.js:** إضافة مكتبة DataTables على جداول Index الثلاثة (السيارات، العملاء، الإيجارات) — ستحصل تلقائيًا على بحث وفرز وترقيم صفحات عربي (بترجمة `language` إلى ar).

بعد هذه التعديلات يعاد البناء والاختبار والدفع إلى فرع جديد مثل `features/modal-datatable-icons` ثم الدمج إلى `master` عبر Pull Request.

### المرحلة 2 — تطبيق Flutter (3–5 أيام)

المتطلب: صفحة رئيسية + ثلاث صفحات إضافية + Drawer + شريط تنقل سفلي + AppBar + BottomSheet + خط مخصص. الخطة المقترحة:

| العنصر | التنفيذ |
|---|---|
| المشروع | `flutter create car_rental_app` ببنية **Clean** مبسطة: `core/` (الثوابت، الخط، الألوان)، `data/api/`، `models/`، `screens/`، `widgets/` |
| الصفحة الرئيسية | قائمة السيارات المتاحة من الـ API (`GET /api/cars`) ببطاقات صورة/ماركة/سعر |
| صفحة تفاصيل السيارة | تفاصيل + زر حجز |
| صفحة الإيجارات | حجزات العميل مع حالتها (شارات ملونة) |
| صفحة الملف/الإعدادات | بيانات المستخدم وتسجيل الخروج |
| Drawer | قائمة تنقل جانبية (الرئيسية، سيارتي، إيجاراتي، تسجيل الخروج) |
| BottomNavigationBar | تنقل سفلي بين الرئيسية/الإيجارات/الملف |
| AppBar | موحد عبر الشاشات بلون العلامة الأزرق |
| ModalBottomSheet | نافذة حجز سريعة أو فلترة السيارات |
| الخط | `google_fonts: ^6.0` مع Tajawal عبر `ThemeData` |
| الاتصال بالـ API | `http` + JWT Token: `/api/auth/login` ثم حفظ التوكن، ورؤوس Authorization على باقي الطلبات |

**ملاحظة مهمة للإنتاج:** الـ API يستعمل JWT، وهذا مثالي لـ Flutter. فقط تأكد أن API يعمل على عنوان يسهل الوصول إليه من المحاكي (`http://10.0.2.2:5109` لـ Android emulator) أو حوّله لرابط نشر عام.

### المرحلة 3 — التوثيق (W2 + W10)

لديك حاليًا `تقرير-تقييم-ولوحة-التحكم.md` و`docs/CarRental.postman_collection.json`. المطلوب إضافته لملف `DOCUMENTATION.md` جديد:

1. **مقترح المشروع (W2):** مقدمة، المشكلة، الأهداف، النطاق، المنهجية (Clean Architecture)، الجدول الزمني، المخاطر — سأعدّه لك جاهزًا بصيغة أكاديمية.
2. **المخططات (W3):** مخطط البنية، Use Case، Sequence، Class Diagram — سأنشئها بصيغة Mermaid داخل المستند بحيث تُصدَّر رسوماتها لأي أداة.
3. **دليل التثبيت:** خطوات Visual Studio 2026 + SQLite البديل (بما فيها حل مشكلة DefineStaticWebAssets التي واجهتها: مسح bin/obj).
4. **دليل المستخدم:** شاشة بشاشة مع لقطات من لوحة التحكم الجديدة.
5. **حالات اختبار الـ API (W6):** سأضيف ملف `docs/API_TEST_CASES.md` يوثّق كل اختبار ونتيجته المتوقعة.

### المرحلة 4 — رفع الجودة لحدّها الأعلى (اختياري — ما بعد الإلزاميات)

بعد ضمان الدرجة الكاملة، هذه التحسينات تجعل المشروع «نموذجيًا» في التقييم:

- **تصوير الشاشات لـ UI/UX (W4):** لقطة لكل شاشة MVC مع Wireframe مبسط في Figma (سأجهز لك wireframes بصريًا).
- **تغطية Unit Tests:** مشروع `CarRental.Tests` يستبدل الاختبارات النصية الحالية بـ xUnit.
- **Rate Limiting و Logging منظم** في الـ API.
- **واجهة عامة للعملاء:** صفحة رئيسية (Landing) لعرض السيارات المتاحة للزوار قبل تسجيل الدخول — يخدم متطلب «Main Web page for displaying the information» إذا كان المقصود صفحة عامة وليست فقط لوحة الإدارة.

---

## رابعًا: ترتيب الفروع المقترح في GitHub (W9)

| الفرع | الغرض |
|---|---|
| `master` | النسخة المستقرة |
| `develop` | خط التطوير الرئيسي |
| `features/modal-datatable-icons` | المرحلة 1 |
| `features/flutter-app` | المرحلة 2 |
| `docs/documentation` | المرحلة 3 |
| `mvc-dashboard` | الواجهة الحالية (موجودة) |

كل فرع يُدمج في `master` عبر **Pull Request** مع رسالة commit واضحة — وهذا يغطي كامل متطلب W9 بامتياز.

---

## خامسًا: الخلاصة والخطوة التالية

المشروع في وضع ممتاز: **البنية الأساسية وWeb API وتطبيق MVC وGitHub مكتملة بأعلى من 60% من درجة الدورة**. المتبقي أربعة أعمال موزونة:

1. إضافات MVC (Icons، خط، Modal، DataTables) — يوم واحد.
2. تطبيق Flutter — 3–5 أيام.
3. وثائق W2/W3/W10 — يوم واحد (أساعدها بالكامل).
4. UI/UX Wireframes — نصف يوم (أساعدها بصريًا).

**ابدأ فورًا بالمرحلة 1 لأنها الأسرع والأكثر ضمانًا للدرجة، وابدأ Flutter بالتوازي.**

إن أردت، أبدأ فورًا بتنفيذ المرحلة 1 (إضافة الأيقونات والخط والـ Modal وDataTables) ثم بناء هيكل تطبيق Flutter كاملًا، وأعدّ لك ملف التوثيق الجامعي الجاهز للنسخ في تقريرك.
